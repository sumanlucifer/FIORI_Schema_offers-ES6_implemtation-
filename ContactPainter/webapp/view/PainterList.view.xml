<mvc:View height="100%"
    xmlns:smartTable="sap.ui.comp.smarttable" controllerName="com.knpl.pragati.ContactPainter.controller.PainterList"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:core="sap.ui.core"
    xmlns:layout="sap.ui.layout"
    xmlns="sap.m"
    xmlns:f="sap.f"
    xmlns:fb="sap.ui.comp.filterbar"
    xmlns:vm="sap.ui.comp.variants"
    xmlns:smartFilterBar="sap.ui.comp.smartfilterbar">
    <f:DynamicPage id="dynamicPageId" headerExpanded="true">
        <f:title>
            <f:DynamicPageTitle>
                <f:heading>
                    <vm:VariantManagement standardItemText="{i18n>appDescription}"></vm:VariantManagement>
                </f:heading>
            </f:DynamicPageTitle>
        </f:title>
        <f:header>
            <f:DynamicPageHeader pinnable="true">
                <f:content>
                    <fb:FilterBar showRestoreButton="true" id="filterbar" reset="onResetFilterBar" search="onFilter" useToolbar="false" showRestoreOnFB="true">
                        <fb:filterGroupItems>
                            <fb:FilterGroupItem groupName="__$INTERNAL$" name="AgeGroup" label="{i18n>AgeGroup}" visibleInFilterBar="true" class="searchFieldLabelHidden">
                                <fb:control>
                                    <ComboBox placeholder="{i18n>Enter} {i18n>AgeGroup}" id="cmbxAgeGrp" items="{path:'/MasterAgeGroupSet'}" selectedKey="{oModelView>/filterBar/AgeGroupId}">
                                        <core:Item text="{AgeGroup}" key="{Id}"/>
                                    </ComboBox>
                                </fb:control>
                            </fb:FilterGroupItem>
                            <fb:FilterGroupItem groupName="__$INTERNAL$" name="StartDate" label="{i18n>StartDate}" visibleInFilterBar="true" class="searchFieldLabelHidden">
                                <fb:control>
                                    <DatePicker placeholder="DD/MM/YYYY" id="idDpickerFilter" value="{path:'oModelView>/filterBar/StartDate', type:'sap.ui.model.type.Date', formatOptions: {pattern:'dd/MM/yyyy', UTC:true } }" displayFormat="dd/MM/yyyy"></DatePicker>
                                </fb:control>
                            </fb:FilterGroupItem>
                            <fb:FilterGroupItem groupName="__$INTERNAL$" name="EndDate" label="{i18n>EndDate}" visibleInFilterBar="true" class="searchFieldLabelHidden">
                                <fb:control>
                                    <DatePicker placeholder="DD/MM/YYYY"  value="{path:'oModelView>/filterBar/EndDate', type:'sap.ui.model.type.Date', formatOptions: {pattern:'dd/MM/yyyy', UTC:true } }" displayFormat="dd/MM/yyyy"></DatePicker>
                                </fb:control>
                            </fb:FilterGroupItem>
                            <fb:FilterGroupItem groupName="__$INTERNAL$" name="Status" label="{i18n>Status}" visibleInFilterBar="true" class="searchFieldLabelHidden">
                                <fb:control>
                                    <ComboBox placeholder="{i18n>Enter} {i18n>Status}" id="cmbx" selectedKey="{oModelView>/filterBar/RegistrationStatus}">
                                        <core:Item text="Pending" key="PENDING"/>
                                        <core:Item text="Registered" key="REGISTERED"/>
                                    </ComboBox>
                                </fb:control>
                            </fb:FilterGroupItem>
                        </fb:filterGroupItems>
                    </fb:FilterBar>
                </f:content>
            </f:DynamicPageHeader>
        </f:header>
        <f:content>
            <Table id="idPainterTable" growing="true" growingThreshold="15" updateFinished="onUpdateFinished" width="auto" items="{path: '/PainterSet',sorter:{path:'CreatedAt',descending:true},filters:[{path:'IsArchived',operator:'EQ',value1:false}], parameters:{expand:'AgeGroup,Dealers,PainterBankDetails,PrimaryDealerDetails,PainterKycDetails,PainterType' }}" noDataText="{i18n>tableNoDataText}" growingScrollToLoad="false" busyIndicatorDelay="{oModelView>/tableBusyDelay}">
                <headerToolbar>
                    <Toolbar>
                        <Title text="{oModelView>/pageTitle}"/>
                        <ToolbarSpacer/>
                        <Button icon="sap-icon://add" text="{i18n>AddPainter}" type="Transparent" press=".onPressAddPainter" tooltip="{i18n>AddPainter}"></Button>
                        <Button icon="sap-icon://sort" type="Transparent" press="handleSortButtonPressed"></Button>
                    </Toolbar>
                </headerToolbar>
                <columns>
                    <Column width="14%" id="nameColumn">
                        <Text text="{i18n>Name}"/>
                    </Column>
                    <Column width="10%">
                        <Text text="{i18n>MembershipId}"/>
                    </Column>
                    <Column>
                        <Text text="{i18n>RegistrationDate}"/>
                    </Column>
                    <Column>
                        <Text text="{i18n>MobileNumber}"/>
                    </Column>
                    <Column >
                        <Text text="{i18n>PainterType}" />
                    </Column>
                    <!-- <Column>
                        <Text text="{i18n>AgeGroup}"/>
                    </Column> -->
                    <Column>
                        <Text text="{i18n>LinkPrimaryDealer}"/>
                    </Column>
                    <Column>
                        <Text text="{i18n>LinkSecondryDealer}"/>
                    </Column>
                    <Column>
                        <Text text="{i18n>KycStatus}"/>
                    </Column>
                    <Column>
                        <Text text="{i18n>BankDetailsStatus}"/>
                    </Column>
                    <Column>
                        <Text text="{i18n>RegStatus}"/>
                    </Column>
                    <Column width="8%">
                        <Text text="{i18n>Action}"/>
                    </Column>
                </columns>

                <items>
                    <ColumnListItem press="onListItemPress" type="Navigation">
                        <cells>
                            <ObjectIdentifier tooltip="{Name}" title="{Name}" titleActive="true"/>
                            <VBox width="100%" >
                                <Text text="{MembershipCard}" visible="{= ${MembershipCard} !== null }"/>
                                <Button tooltip="Generate Membership Id" width="100%" text="{i18n>GenerateMId}" type="Ghost" press="onGenMemId"  visible="{= ${MembershipCard}===null }"/>
                            </VBox>
                            
                            <Text text="{path:'CreatedAt',type:'sap.ui.model.type.Date',formatOptions:{pattern:'dd/MM/yyyy'} }"/>
                            <Text text="{Mobile}"/>
                            <Text text="{PainterType/PainterType}" />
                            <!-- <Text text="{AgeGroup/AgeGroup}"/> -->
                            <Text text="{PrimaryDealerDetails/DealerName}"/>
                            <Link text="{=${Dealers}.length} {i18n>Dealers}" press="onDealersPress"/>
                            <Text text="{path:'PainterKycDetails/Status',formatter:'.fmtStatus'}"/>
                            <Text text="{path:'PainterBankDetails/Status',formatter:'.fmtStatus'}"/>
                            <ObjectStatus text="{path:'RegistrationStatus',formatter:'.fmtStatus'}" state="{= ${RegistrationStatus} === 'PENDING' ? 'None' : 'Success' }"/>
                            <HBox>
                                <Button icon="sap-icon://edit" type="Transparent" press="onListItemPress"></Button>
                                <Button icon="sap-icon://delete" type="Transparent" press="onDeactivate"></Button>
                            </HBox>
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </f:content>
    </f:DynamicPage>
</mvc:View>

<core:FragmentDefinition xmlns:html="http://www.w3.org/1999/xhtml"
    xmlns:smartForm="sap.ui.comp.smartform"
    xmlns:smartField="sap.ui.comp.smartfield"
    xmlns:app="http://schemas.sap.com/sapui5/extension/sap.ui.core.CustomData/1"
    xmlns="sap.m"
    xmlns:l="sap.ui.layout"
    xmlns:f="sap.ui.layout.form"
    xmlns:core="sap.ui.core">

    <Table width="auto" id="idLoyaltyPoints" items="{path: '/PainterPointsHistorySet', sorter:{path:'CreatedAt',descending:true} }" noDataText="{i18n>tableNoDataText}" growingScrollToLoad="false" busyIndicatorDelay="{oModelView>/tableBusyDelay}">
        <headerToolbar>
            <Toolbar>
                <SegmentedButton selectedKey="0" selectionChange="onLoyaltySelChange">
                    <items>
                        <SegmentedButtonItem text="{i18n>Accural}" key="0"/>
                        <SegmentedButtonItem text="{i18n>Redemption}" key="1"/>
                    </items>
                </SegmentedButton>
            </Toolbar>
        </headerToolbar>
        <columns>
            <Column>
                <Text text="{i18n>Type}"/>
            </Column>
            <Column>
                <Text text="{i18n>EarnedPoints}"/>
            </Column>
            <Column>
                <Text text="{i18n>TotalPoints}"/>
            </Column>
            <Column>
                <Text text="{i18n>DateAndTime}"/>
            </Column>
        </columns>

        <items>
            <ColumnListItem press="onListItemPress">
                <cells>
                    <Text text="{path:'PointType',formatter:'.fmtLowerCase'}" />
                    <Text text="{RewardPoints}" />
                    <Text text="{TotalPoints}" />
                    <Text text="{path:'CreatedAt',type:'sap.ui.model.type.Date',formatOptions:{pattern:'dd/MM/YYYY h:MM a'}}"/>
                </cells>
            </ColumnListItem>
        </items>
    </Table>

</core:FragmentDefinition>

<mvc:View
 xmlns:core="sap.ui.core"
	controllerName="com.knpl.pragati.portfolio.controller.LandingPage"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc"
	>

 <Page busy="{oModelControl>/PageBusy}"  title="{i18n>appTitle}" busyIndicatorDelay="0" showNavButton="false" showHeader="true" >
        <!-- <headerContent>
            <Bar>
                <contentLeft>
                    <Breadcrumbs currentLocationText="{i18n>AddSite}">
                        <Link text="{i18n>site}" press="onNavToHome"/>
                    </Breadcrumbs>
                </contentLeft>
            </Bar>
        </headerContent> -->
        <content>
            <VBox width="100%" id="oVBoxObjectPage">
            </VBox>
        </content>
        <footer>
            <Toolbar>
                <ToolbarSpacer/>
                <Button visible="false" width="7%" text="{i18n>Cancel}" type="Ghost" press="onNavToHome"/>
                <Button width="7%" text="{i18n>Generate}" type="Emphasized" press="onPressSave"/>
            </Toolbar>
        </footer>
    </Page>


</mvc:View>

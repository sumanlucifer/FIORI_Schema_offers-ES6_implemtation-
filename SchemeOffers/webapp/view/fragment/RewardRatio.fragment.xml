<core:FragmentDefinition xmlns="sap.m"
    xmlns:l="sap.ui.layout"
    xmlns:u="sap.ui.unified"
    xmlns:f="sap.ui.layout.form"
    xmlns:core="sap.ui.core">
    <l:VerticalLayout>
        <!-- <f:Form editable="true">
            <f:layout>
                <f:ResponsiveGridLayout labelSpanXL="12" labelSpanL="12" labelSpanM="12" labelSpanS="12" adjustLabelSpan="false" emptySpanXL="0" emptySpanL="0" emptySpanM="0" emptySpanS="0" columnsXL="3" columnsL="3" columnsM="3" singleContainerFullSize="false" />
            </f:layout>
            <f:formContainers>
                <f:FormContainer  >
                    <f:formElements>
                        <f:FormElement >
                            <f:fields>

                            </f:fields>
                        </f:FormElement>
                    

                    </f:formElements>
                </f:FormContainer>
            </f:formContainers>
        </f:Form> -->
        <!-- <Table id="Table1" visible="{= ${oModelControl>/Rbtn/Rewards}===0}" class="sapUiMediumMarginTop" width="auto" items="{path: 'oModelControl>/Table/Table1',  templateShareable:false}" noDataText="{i18n>tableNoDataText}" growingScrollToLoad="false" busyIndicatorDelay="{oModelView>/tableBusyDelay}">
            <headerToolbar>
                <Toolbar>
                    <Title text="{i18n>formApplicableLbl10}1" />
                    <ToolbarSpacer />
                    <Button enabled="{= ${oModelControl>/Table/Table1}.length &lt; 1 }" icon="sap-icon://add" text="{i18n>Add}" tooltip="{i18n>Add}" type="Emphasized" press="onPressAddGenericRewardsV1('add')"/>
                </Toolbar>
            </headerToolbar>

            <columns>
               
                <Column>
                    <Label text="{i18n>RequiredVolume}" required="false" />
                </Column>
                <Column>
                    <Text text="{i18n>RequiredPoints}" />
                </Column>
                <Column>
                    <Label text="{i18n>Table1Label4}" required="false"/>
                </Column>
              
                <Column>
                    <Text text="{i18n>Table1Label6}" />
                </Column>
                <Column>
                    <Text text="{i18n>tblColAction}"/>
                </Column>
            </columns>

            <items>
                <ColumnListItem press="onListItemPress">
                    <cells>


                    
                        <VBox >
                            <Label visible="{= ${oModelControl>editable} === false }" text="{path:'oModelControl>RequiredVolume',formatter:'.formatter.fmtCheckNull'}" />
                            <Input visible="{= ${oModelControl>editable} === true }" type="Number" required="false" placeholder="{i18n>placeHolder3}" width="100%" value="{path:'oModelControl>RequiredVolume',type: 'sap.ui.model.type.String',constraints:{search:'^[0-9]+$',minLength:1,maxLength:6} }" />
                        </VBox>
                        <VBox >
                            <Label visible="{= ${oModelControl>editable} === false }" text="{path:'oModelControl>RequiredPoints',formatter:'.formatter.fmtCheckNull'}" />
                            <Input visible="{= ${oModelControl>editable} === true }" placeholder="{i18n>placeHolder3}" type="Number" width="100%" value="{path:'oModelControl>RequiredPoints',type: 'sap.ui.model.type.String',constraints:{search:'^[0-9]+$',minLength:1,maxLength:6} }" />
                        </VBox>
                        <VBox >
                            <Label visible="{= ${oModelControl>editable} === false }" text="{path:'oModelControl>RewardPoints',formatter:'.formatter.fmtCheckNull'}" />
                            <Input visible="{= ${oModelControl>editable} === true }" type="Number" required="true" placeholder="{i18n>placeHolder3}" width="100%" value="{path:'oModelControl>RewardPoints',type: 'sap.ui.model.type.String',constraints:{search:'^[0-9]+$',minLength:1,maxLength:6} }" />
                        </VBox>
                        <VBox >
                            <Label visible="{= ${oModelControl>editable} === false }" text="{path:'oModelControl>RewardCash',formatter:'.formatter.fmtCheckNull'}" />
                            <Input visible="{= ${oModelControl>editable} === true }" type="Number" placeholder="{i18n>placeHolder3}" width="100%" value="{path:'oModelControl>RewardCash',type: 'sap.ui.model.type.String',constraints:{search:'^[0-9]',minLength:1,maxLength:6} }" />
                        </VBox>



                 

                        <HBox>
                            <Button visible="{= ${oModelView>editable} !== true }" icon="sap-icon://edit" tooltip="{i18n>Edit}" type="Transparent" press="onPressAddRewards"></Button>
                            <Button visible="{= ${oModelView>editable} === true }" text="{i18n>Save}" tooltip="{i18n>Edit}" type="Transparent" press="onPressSaveReward"></Button>
                            <Button icon="sap-icon://delete" type="Transparent" tooltip="{i18n>Delete}" press="onRemovedReward"></Button>
                        </HBox>
                    </cells>
                </ColumnListItem>
            </items>
        </Table> -->

        <MessageStrip text="{i18n>Meesage4}" showIcon="true"/>
        <Table id="Table2" items="{path: 'oModelControl>/Table/Table2',  templateShareable:false}" width="auto" noDataText="{i18n>tableNoDataText}" growingScrollToLoad="false" busyIndicatorDelay="{oModelView>/tableBusyDelay}">
            <headerToolbar>
                <Toolbar>
                    <Title text="{i18n>Slab}"/>
                    <RadioButtonGroup columns="2" select=".onRbRRDialogVolume" selectedIndex="{oModelView>/InputType}">
                        <buttons>
                            <RadioButton text="{i18n>RequiredVolume}"/>
                            <RadioButton text="{i18n>Table1Label2}"/>
                        </buttons>
                    </RadioButtonGroup>
                    <ToolbarSpacer />
                    <Button icon="sap-icon://add" text="{i18n>Add}" tooltip="{i18n>Add}" type="Emphasized" press="onPressAddRewardsV1('add')"/>
                </Toolbar>
            </headerToolbar>

            <columns>

                <Column visible="{= ${oModelView>/InputType}===0 }">
                    <Label text="{i18n>RequiredVolume}" visible="true" required="true"/>
                </Column>
                <Column visible="{= ${oModelView>/InputType}===1 }">
                    <Label text="{i18n>Table1Label2}" required="true" />
                </Column>
                <Column>
                    <Label text="{i18n>Table1Label4}" />
                </Column>
                <Column>
                    <Text text="{i18n>Table1Label6}" />
                </Column>
                <Column>
                    <Text text="{i18n>Table1Label5}" />
                </Column>
                <Column>
                    <Text text="{i18n>tblColAction}"/>
                </Column>
            </columns>

            <items>
                <ColumnListItem press="onListItemPress">
                    <cells>

                        <!-- <VBox>
                            <Label visible="{= ${oModelControl>/Rbtn/AppPacks1}=== 1 }" text="{path:'oModelControl>SkuCode',formatter:'.GetPackName'}" />
                            <Label visible="{= ${oModelControl>/Rbtn/AppPacks1}=== 0 }" text="{path:'oModelControl>ProductCode',formatter:'.GetProdName'}" />
                        </VBox> -->
                        <!-- <VBox >
                            <Select editable="{= ${oModelControl>editable} === true }" selectedKey="{oModelControl>Type}">
                                <items>
                                    <core:Item text="Required Volumne" key="1" />
                                    <core:Item text="Required Points" key="2" />
                                </items>
                            </Select>
                        </VBox> -->
                        <VBox visible="{= ${oModelView>/InputType}===0 }">
                            <Label visible="{= ${oModelControl>editable} !== true }" text="{path:'oModelControl>RequiredVolume',formatter:'.formatter.fmtCheckNull'}" />
                            <Input visible="{= ${oModelControl>editable} === true }" required="true" type="Number" placeholder="{i18n>placeHolder3}" width="100%" value="{path:'oModelControl>RequiredVolume',type: 'sap.ui.model.type.String',constraints:{search:'^[1-9][0-9]*$',minLength:1,maxLength:6} }" valueStateText="Kindly Enter a Valid Number not starting with 0" />
                        </VBox>
                        <VBox visible="{= ${oModelView>/InputType}===1 }">
                            <Label visible="{= ${oModelControl>editable} !== true  }" text="{path:'oModelControl>RequiredPoints',formatter:'.formatter.fmtCheckNull'}" />
                            <Input visible="{= ${oModelControl>editable} === true }" required="true" placeholder="{i18n>placeHolder3}" type="Number" width="100%" value="{path:'oModelControl>RequiredPoints',type: 'sap.ui.model.type.String',constraints:{search:'^[1-9][0-9]*$',minLength:1,maxLength:6} }" valueStateText="Kindly Enter a Valid Number not starting with 0" />
                        </VBox>
                        <VBox >
                            <Label visible="{= ${oModelControl>editable} !== true  }" text="{path:'oModelControl>RewardPoints',formatter:'.formatter.fmtCheckNull'}" />
                            <Input visible="{= ${oModelControl>editable} === true }" type="Number" placeholder="{i18n>placeHolder3}" width="100%" value="{path:'oModelControl>RewardPoints',type: 'sap.ui.model.type.String',constraints:{search:'^[1-9][0-9]*$',minLength:1,maxLength:6} }" valueStateText="Kindly Enter a Valid Number not starting with 0" />
                        </VBox>
                        <VBox >
                            <Label visible="{= ${oModelControl>editable} !== true  }" text="{path:'oModelControl>RewardCash',formatter:'.formatter.fmtCheckNull'}" />
                            <Input visible="{= ${oModelControl>editable} === true }" type="Number" placeholder="{i18n>placeHolder3}" width="100%" value="{path:'oModelControl>RewardCash',type: 'sap.ui.model.type.String',constraints:{search:'^[1-9][0-9]*$',minLength:1,maxLength:6} }" valueStateText="Kindly Enter a Valid Number not starting with 0" />
                        </VBox>
                        <VBox >
                            <Label visible="{= ${oModelControl>editable} !== true  }" text="{path:'oModelControl>RewardGiftName',formatter:'.formatter.fmtCheckNull'}" tooltip="{oModelControl>RewardGiftName}"/>
                            <Input visible="{= ${oModelControl>editable} === true }" placeholder="{i18n>placeHolder3}" width="100%" maxLength="100" value="{path:'oModelControl>RewardGiftName',type: 'sap.ui.model.type.String',constraints:{search :'^[a-zA-Z0-9]{1}[wW]*',minLength:1,maxLength:100} }" valueStateText="Kindly Enter a Valid Text of max 50 characters" />
                        </VBox>



                        <!-- <ComboBox editable="false" placeholder="{i18n>placeHolder1}" width="80%" items="{path:'oModelControl>/oData/Rewards',type:'.cmbxDtype2', templateShareable:false}" selectedKey="{path:'oModelControl>RewardGiftId',type:'.cmbxDtype2'}">
                                <core:Item text="{oModelControl>Name}" key="{oModelControl>key}"/>
                           </ComboBox> -->

                        <HBox>
                            <Button visible="{= ${oModelControl>editable} !== true }" icon="sap-icon://edit" tooltip="{i18n>Edit}" type="Transparent" press="onPressAddRewardsV1"></Button>
                            <Button visible="{= ${oModelControl>editable} === true }" text="{i18n>Save}" tooltip="{i18n>Save}" type="Transparent" press="onPressSaveReward"></Button>
                            <Button icon="sap-icon://delete" type="Transparent" tooltip="{i18n>Delete}" press="onRemovedReward"></Button>
                        </HBox>

                    </cells>
                </ColumnListItem>
            </items>
        </Table>

        <VBox visible="{oModelControl>/OfferType/EarnedPointsCondition}">
            <items>
                <Title titleStyle="H4" text="{i18n>Condotion1EarnedPoints}" class="sapUiMediumMarginTop" />

                <RadioButtonGroup columns="2" select=".onRbConditions1" selectedIndex="{= ${oModelView>/OfferConditions/IsEarnedPointsCondition} === false ? 0:1}">
                    <buttons>
                        <RadioButton text="{i18n>No}"/>
                        <RadioButton text="{i18n>Yes}"/>
                    </buttons>
                </RadioButtonGroup>
                <Table id="Table5" visible="{oModelView>/OfferConditions/IsEarnedPointsCondition}"  items="{oModelControl>/Table/Table5}" width="auto" noDataText="{i18n>tableNoDataText}">
                    <headerToolbar>
                        <Toolbar>
                            <Title text="{i18n>EarnedPoints}" />
                            <ToolbarSpacer />
                            <Button icon="sap-icon://add" text="{i18n>Add}" tooltip="{i18n>Add}" type="Emphasized" press="onPressAddCondition1('add')"/>
                        </Toolbar>
                    </headerToolbar>

                    <columns>
                        <Column>
                            <Text text="{i18n>EndDate}" />
                        </Column>

                        <Column visible="{= ${oModelView>/InputType}===0 }">
                            <Text text="{i18n>VolumeToBeCompleted}" />
                        </Column>
                        <Column visible="{= ${oModelView>/InputType}===1 }">
                            <Text text="{i18n>PointsToComplete}" />
                        </Column>
                        <Column>
                            <Text text="{i18n>tblColAction}"/>
                        </Column>
                    </columns>

                    <items>
                        <ColumnListItem >
                            <cells>
                                <VBox >
                                    <Label visible="{= ${oModelControl>editable} !== true }" text="{path:'oModelControl>EndDate',type:'sap.ui.model.type.Date', formatOptions: { pattern: 'dd/MM/yyyy', strictParsing: true} }"/>
                                    <DatePicker change=".onEndDateCondition1" visible="{= ${oModelControl>editable} === true }" displayFormat="dd/MM/yyyy" placeholder="DD/MM/YYYY" width="50%" dateValue="{oModelControl>EndDate}" />
                                </VBox>
                                <VBox visible="{= ${oModelView>/InputType}===0 }">
                                    <Label visible="{= ${oModelControl>editable} !== true  }" text="{path:'oModelControl>RequiredVolume',formatter:'.formatter.fmtCheckNull'}" />
                                    <Input visible="{= ${oModelControl>editable} === true }" required="true" placeholder="{i18n>placeHolder3}" width="50%" maxLength="50" value="{path:'oModelControl>RequiredVolume',type: 'sap.ui.model.type.String',constraints:{search :'^[1-9][0-9]*$',minLength:1,maxLength:6} }" valueStateText="Kindly Enter a Valid Number not starting with 0" />
                                </VBox>
                                <VBox visible="{= ${oModelView>/InputType}===1 }">
                                    <Label visible="{= ${oModelControl>editable} !== true  }" text="{path:'oModelControl>RequiredPoints',formatter:'.formatter.fmtCheckNull'}" />
                                    <Input visible="{= ${oModelControl>editable} === true }" required="true" placeholder="{i18n>placeHolder3}" width="50%" maxLength="50" value="{path:'oModelControl>RequiredPoints',type: 'sap.ui.model.type.String',constraints:{search :'^[1-9][0-9]*$',minLength:1,maxLength:6} }" valueStateText="Kindly Enter a Valid Number not starting with 0" />
                                </VBox>
                                <HBox>
                                    <Button visible="{= ${oModelControl>editable} !== true }" icon="sap-icon://edit" tooltip="{i18n>Edit}" type="Transparent" press="onPressAddCondition1"></Button>
                                    <Button visible="{= ${oModelControl>editable} === true }" text="{i18n>Save}" tooltip="{i18n>Save}" type="Transparent" press="onPressSaveCondition1"></Button>
                                    <Button icon="sap-icon://delete" type="Transparent" tooltip="{i18n>Delete}" press="onRemovedCondition"></Button>
                                </HBox>

                            </cells>
                        </ColumnListItem>
                    </items>
                </Table>
            </items>
        </VBox>
        <VBox visible="{oModelControl>/OfferType/ProductValueCondition}" >
            <items>
                <Title titleStyle="H4" text="{i18n>Condition2ProdValue}" class="sapUiMediumMarginTop" />
                <RadioButtonGroup columns="2" select=".onRbConditions1" selectedIndex="{= ${oModelView>/OfferConditions/IsProductValueCondition} === false ? 0:1}">
                    <buttons>
                        <RadioButton text="{i18n>No}"/>
                        <RadioButton text="{i18n>Yes}"/>
                    </buttons>
                </RadioButtonGroup>
                <Table  id="Table6" visible="{oModelView>/OfferConditions/IsProductValueCondition}" items="{path: 'oModelControl>/Table/Table6',  templateShareable:false}" width="auto" noDataText="{i18n>tableNoDataText}" busyIndicatorDelay="{oModelView>/tableBusyDelay}">
                    <headerToolbar>
                        <Toolbar>
                            <Title text="{i18n>ProuctValueCondition}"/>
                            <ToolbarSpacer />
                            <Button icon="sap-icon://add" text="{i18n>Add}" tooltip="{i18n>Add}" type="Emphasized" press="onPressAddCondition2('add')"/>
                        </Toolbar>
                    </headerToolbar>
                    <columns>


                        <Column>
                            <Text text="{i18n>Products}" />
                        </Column>
                        <Column>
                            <Text text="{i18n>Percentage}" />
                        </Column>
                        <Column>
                            <Text text="{i18n>tblColAction}"/>
                        </Column>
                    </columns>

                    <items>
                        <ColumnListItem press="onListItemPress">
                            <cells>

                                <!-- <VBox>
                            <Label visible="{= ${oModelControl>/Rbtn/AppPacks1}=== 1 }" text="{path:'oModelControl>SkuCode',formatter:'.GetPackName'}" />
                            <Label visible="{= ${oModelControl>/Rbtn/AppPacks1}=== 0 }" text="{path:'oModelControl>ProductCode',formatter:'.GetProdName'}" />
                        </VBox> -->
                                <!-- <VBox >
                            <Select editable="{= ${oModelControl>editable} === true }" selectedKey="{oModelControl>Type}">
                                <items>
                                    <core:Item text="Required Volumne" key="1" />
                                    <core:Item text="Required Points" key="2" />
                                </items>
                            </Select>
                        </VBox> -->
                                <VBox >
                                    <Label visible="{= ${oModelControl>editable} !== true  }" text="{path:'oModelControl>ProductCode',formatter:'.GetProdName'}" />
                                    <Input visible="{= ${oModelControl>editable} === true }" showValueHelp="true" valueHelpOnly="true" valueHelpRequest=".onValueHelpProductsTable" placeholder="{i18n>placeHolder3}" width="50%" value="{path:'oModelControl>ProductCode',formatter:'.formatter.fmtGetProductName'}" valueStateText="Kindly Enter a Valid Number not starting with 0" />
                                </VBox>
                                <VBox >
                                    <Label visible="{= ${oModelControl>editable} !== true  }" text="{path:'oModelControl>Percentage',formatter:'.formatter.fmtCheckNull'}" />
                                    <Input visible="{= ${oModelControl>editable} === true }" placeholder="{i18n>placeHolder3}" width="50%" maxLength="50" value="{path:'oModelControl>Percentage',type: 'sap.ui.model.type.String',constraints:{search :'^[1-9]',minLength:1,maxLength:6} }" valueStateText="Kindly Enter a Valid Text of max 50 characters" />
                                </VBox>
                                <HBox>
                                    <Button visible="{= ${oModelControl>editable} !== true }" icon="sap-icon://edit" tooltip="{i18n>Edit}" type="Transparent" press="onPressAddCondition2"></Button>
                                    <Button visible="{= ${oModelControl>editable} === true }" text="{i18n>Save}" tooltip="{i18n>Save}" type="Transparent" press="onPressSaveCondition2"></Button>
                                    <Button icon="sap-icon://delete" type="Transparent" tooltip="{i18n>Delete}" press="onRemovedCondition"></Button>
                                </HBox>

                            </cells>
                        </ColumnListItem>
                    </items>
                </Table>
            </items>
        </VBox>
        <VBox visible="{oModelControl>/OfferType/RedemptionCycleCondition}"  class="sapUiMediumMarginTop">
            <items>
                <Title titleStyle="H4" text="{i18n>Condotion3redemptionCycle}" />
                <RadioButtonGroup columns="2" select=".onRbConditions1" selectedIndex="{= ${oModelView>/OfferConditions/IsRedemptionCycleCondition} === false ? 0:1}">
                    <buttons>

                        <RadioButton text="{i18n>No}"/>
                        <RadioButton text="{i18n>Yes}"/>
                    </buttons>
                </RadioButtonGroup>
                <Table  id="Table7" visible="{oModelView>/OfferConditions/IsRedemptionCycleCondition}" items="{path: 'oModelControl>/Table/Table7',  templateShareable:false}" width="auto" noDataText="{i18n>tableNoDataText}" busyIndicatorDelay="{oModelView>/tableBusyDelay}">
                    <headerToolbar>
                        <Toolbar>
                            <Title text="{i18n>RedemptionCycle}"/>
                            <ToolbarSpacer />
                            <Button icon="sap-icon://add" text="{i18n>Add}" tooltip="{i18n>Add}" type="Emphasized" press="onPressAddCondition3('add')"/>
                        </Toolbar>
                    </headerToolbar>

                    <columns>


                        <Column>
                            <Text text="{i18n>RedemptionCycle}" />
                        </Column>
                        <Column>
                            <Text text="{i18n>Percentage}" />
                        </Column>
                        <Column>
                            <Text text="{i18n>tblColAction}"/>
                        </Column>
                    </columns>

                    <items>
                        <ColumnListItem press="onListItemPress">
                            <cells>


                                <Label text="{path:'oModelControl>RedemptionCycle',formatter:'.formatter.fmtCheckNull'}" />
                                <VBox >
                                    <Label visible="{= ${oModelControl>editable} !== true  }" text="{path:'oModelControl>Percentage',formatter:'.formatter.fmtCheckNull'}" />
                                    <Input required="true" visible="{= ${oModelControl>editable} === true }" placeholder="{i18n>placeHolder3}" width="50%" maxLength="50" value="{path:'oModelControl>Percentage',type: 'sap.ui.model.type.String',constraints:{search :'^[1-9][0-9]*$',minLength:1,maxLength:6} }" valueStateText="Kindly Enter a Valid Text of max 50 characters" />
                                </VBox>
                                <HBox>
                                    <Button visible="{= ${oModelControl>editable} !== true }" icon="sap-icon://edit" tooltip="{i18n>Edit}" type="Transparent" press="onPressAddCondition3"></Button>
                                    <Button visible="{= ${oModelControl>editable} === true }" text="{i18n>Save}" tooltip="{i18n>Save}" type="Transparent" press="onPressSaveCondition3"></Button>
                                    <Button icon="sap-icon://delete" type="Transparent" tooltip="{i18n>Delete}" press="onRemovedCondition"></Button>
                                </HBox>

                            </cells>
                        </ColumnListItem>
                    </items>
                </Table>
            </items>
        </VBox>



    </l:VerticalLayout>


</core:FragmentDefinition>
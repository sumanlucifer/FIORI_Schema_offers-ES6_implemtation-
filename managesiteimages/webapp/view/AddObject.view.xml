<!-- <mvc:View
 xmlns:core="sap.ui.core"
	controllerName="com.knpl.pragati.managesiteimages.controller.AddObject"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc"
	>

 <Page busy="{oModelControl>/PageBusy}"  busyIndicatorDelay="0" showNavButton="false" showHeader="true" >
        <headerContent>
            <Bar>
                <contentLeft>
                    <Breadcrumbs currentLocationText="{i18n>AddPortfolioImage}">
                        <Link text="{i18n>PortfolioImage}" press="onNavToHome"/>
                    </Breadcrumbs>
                </contentLeft>
            </Bar>
        </headerContent>
        <content>
            <VBox width="100%" id="oVBoxAddObjectPage">
            </VBox>
        </content>
        <footer>
            <Toolbar>
                <ToolbarSpacer/>
                <Button width="7%" text="{i18n>Cancel}" type="Ghost" press="onNavToHome"/>
                <Button width="7%" text="{i18n>Save}" type="Emphasized" press="onPressSave"/>
            </Toolbar>
        </footer>
    </Page>
</mvc:View> -->

<mvc:View height="100%" controllerName="com.knpl.pragati.managesiteimages.controller.AddObject" xmlns:mvc="sap.ui.core.mvc" xmlns:core="sap.ui.core" xmlns:layout="sap.ui.layout" xmlns="sap.m" xmlns:f="sap.f" xmlns:fb="sap.ui.comp.filterbar" xmlns:vm="sap.ui.comp.variants" xmlns:smartFilterBar="sap.ui.comp.smartfilterbar">
    <f:DynamicPage busy="{oModelDisplay>/PageBusy}" busyIndicatorDelay="0"	stickySubheaderProvider="iconTabBar"
			class="sapUiNoContentPadding" showFooter="true" id="dynamicPageId" headerExpanded="true">
        <f:title>
            <f:DynamicPageTitle>
                <f:breadcrumbs>
                    <Breadcrumbs currentLocationText="{i18n>AddPainterPortfolio}">
                        <Link text="{i18n>worklistViewTitle}" press="onNavToHome"/>
                    </Breadcrumbs>
                </f:breadcrumbs>
            </f:DynamicPageTitle>
        </f:title>
        <f:header>
            <f:DynamicPageHeader pinnable="true">
                <layout:HorizontalLayout allowWrapping="false">
                    <layout:VerticalLayout class="sapUiSmallMarginEnd">
                        <!-- <ObjectAttribute title="{i18n>PainterName}" text="{Painter/Name}"/>
                        <ObjectAttribute title="{i18n>MembershipId}" text="{Painter/MembershipCard}"/> -->
                        <Label text="{i18n>PrimaryMobileNo}" />
                        <Input required="true" valueHelpOnly="true" showValueHelp="true" valueHelpRequest="onValueHelpRequest" value="{path:'oModelControl>/Mobile', type : 'sap.ui.model.type.String', constraints:{ minLength:1 } }" />
                        <Label text="{i18n>PainterName}" />
                        <Text  text="{oModelControl>/Name}" />
                        <Label text="{i18n>MembershipId}" />
                        <Text text="{oModelControl>/MembershipCard}" />
                        <!-- Zone Division Depot Info -->
                        <Label text="{i18n>Zone}" />
                        <Text text="{oModelControl>/ZoneId}" />
                        <Label text="{i18n>Division}" />
                        <Text text="{oModelControl>/DivisionId}" />
                        <Label text="{i18n>Depot}" />
                        <Text text="{oModelControl>/Depot}" />
                    </layout:VerticalLayout>
                </layout:HorizontalLayout>
            </f:DynamicPageHeader>
        </f:header>
        <f:content>
            <IconTabBar  select="onIcnTbarChange" items="{path:'/PortfolioCategorySet', filters : [ { path : 'IsArchived' , operator : 'EQ' , value1 : false } ], parameters:{select:'Id, category' } }" selectedKey="{oModelView>/PortfolioCategoryId}" expandable="false" expanded="true" id="iconTabBar" class="sapUiResponsiveContentPadding" >
                <items>
                    <IconTabFilter  text="{category}" key="{Id}" >
                        <VBox id="oVBoxAddObjectPage" width="100%"></VBox>  
                    </IconTabFilter>
                </items>
            </IconTabBar>
        </f:content>
        <f:footer>
            <OverflowToolbar>
              <ToolbarSpacer/>
              <Button press="onNavToHome" type="Ghost"  text="{i18n>Cancel}"  />
              <Button press="onPressSave" text="{i18n>Save}" type="Emphasized" />
            </OverflowToolbar>
        </f:footer>
    </f:DynamicPage>
</mvc:View>
<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core">
    <Dialog title="{i18n>banerImage}" afterClose="onDialogClose">
        <content>
            <Carousel class="sapUiContentPadding" loop="true">
                <Image src="{path : 'oModelControl2>/oImage' }" alt="No Banner Image found"/>
            </Carousel>
        </content>
        <endButton>
            <Button text="Close" press="onPressCloseImageDialog" />
        </endButton>
    </Dialog>
</core:FragmentDefinition>

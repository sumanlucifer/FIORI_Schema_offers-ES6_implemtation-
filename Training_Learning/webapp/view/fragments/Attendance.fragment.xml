<core:FragmentDefinition xmlns:html="http://www.w3.org/1999/xhtml" xmlns:u="sap.ui.unified" xmlns:smartForm="sap.ui.comp.smartform" xmlns:smartField="sap.ui.comp.smartfield" xmlns:app="http://schemas.sap.com/sapui5/extension/sap.ui.core.CustomData/1" xmlns="sap.m" xmlns:l="sap.ui.layout" xmlns:f="sap.ui.layout.form" xmlns:core="sap.ui.core">
    <VBox width="100%">
        <Table id="idTblAttendance" width="auto" items="{ path: '/PainterTrainingSet', parameters : { expand : 'PainterDetails' }, sorter: { path: 'UpdatedAt', descending: true } }" noDataText="{i18n>AttendancetableNoDataText}" growingScrollToLoad="false" busyIndicatorDelay="{oModelView>/tableBusyDelay}" growing="true" growingThreshold="20">
            <headerToolbar>
                <Toolbar>
                    <!-- <u:FileUploader
                        id="idFileUpload"
                        name="fileUploader"
                        width="70%"
                        tooltip="{i18n>fileUploaderTrainingtip}"
                        change="oAttendanceFileBrowser"
                        typeMissmatch="handleTypeMissmatch"
                        fileType="pdf"
                        useMultipart="false"
                        sendXHR="true"
                        uploadComplete="handleUploadComplete" /> -->
                        <u:FileUploader maximumFileSize="2" fileAllowed="onUploadAttendance" tooltip="{i18n>btnUpload}" buttonOnly="true"
					    buttonText="{i18n>fileUploaderAttendance}"/>
                        <ToolbarSpacer/>
                        <!-- <Button text="{i18n>btnUpload}" icon="sap-icon://upload" press="onUploadAttendance"> -->
							<layoutData>
								<OverflowToolbarLayoutData maxWidth="200px" priority="NeverOverflow"/>
							</layoutData>
						<!-- </Button> -->
                        <SearchField width="20%" id="searchAttendance"></SearchField>
                </Toolbar>
            </headerToolbar>

            <columns>
                <Column>
                    <Text text="{i18n>colMembershipId}"/>
                </Column>
                <Column>
                    <Text text="{i18n>colPainterName}"/>
                </Column>
                <Column>
                    <Text text="{i18n>colMobileNumber}"/>
                </Column>
                <Column>
                    <Text text="{i18n>colAttendanceDate}"/>
                </Column>
            </columns>

            <items>
                <ColumnListItem press="onListItemPress">
                    <cells>
                        <!-- <Text text="{PainterDetails/MembershipCard}" titleActive="true"/> -->
                        <ObjectIdentifier title="{PainterDetails/MembershipCard}" titleActive="true"/>
                        <Text text="{PainterDetails/Name}"/>
                        <Text text="{PainterDetails/Mobile}"/>
                        <Text text="{path:'AttendedAt',type:'sap.ui.model.type.Date',formatOptions:{pattern:'dd/MM/YYYY'}}"/>
                        <!-- <Text text="{parts: [ {path: 'AttendedAt'} ], formatter : '.formatter.formatDate' }"/> -->
                    </cells>
                </ColumnListItem>
            </items>
        </Table>

    </VBox>
</core:FragmentDefinition>

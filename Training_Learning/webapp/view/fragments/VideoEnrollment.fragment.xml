<core:FragmentDefinition xmlns:html="http://www.w3.org/1999/xhtml" xmlns:smartForm="sap.ui.comp.smartform" xmlns:smartField="sap.ui.comp.smartfield" xmlns:app="http://schemas.sap.com/sapui5/extension/sap.ui.core.CustomData/1" xmlns="sap.m" xmlns:l="sap.ui.layout" xmlns:f="sap.ui.layout.form" xmlns:core="sap.ui.core">
    <VBox width="100%">
        <Table id="idVdTblEnrollment" width="auto" items="{ path: '/PainterLearningSet', parameters : { expand : 'PainterDetails/PrimaryDealerDetails' }, sorter: { path: 'CreatedAt', descending: false } }" noDataText="{i18n>tableNoData}" growingScrollToLoad="false" busyIndicatorDelay="{oModelView>/tableBusyDelay}" growing="true" growingThreshold="15">
            <headerToolbar>
                <Toolbar>
                    <ToolbarSpacer/>
                    <!-- <Button text="{i18n>btnDownloadCSV}" icon="sap-icon://download" press="onExportViews" type="Emphasized"/> -->
                     <SearchField width="20%" showSearchButton="true" search="onViewsSearch" value="{oModelView>/Search/Enrollment}"></SearchField>
                </Toolbar>
            </headerToolbar>

            <columns>
                <Column>
                    <Text text="{i18n>colName}"/>
                </Column>
                <Column>
                    <Text text="{i18n>colMembershipId}"/>
                </Column>
                <Column>
                    <Text text="{i18n>colMobileNumber}"/>
                </Column>
                <Column>
                    <Text text="{i18n>colZone}"/>
                </Column>
                <Column>
                    <Text text="{i18n>colDivision}"/>
                </Column>
                <Column>
                    <Text text="{i18n>colDepot}"/>
                </Column>
                <Column>
                    <Text text="{i18n>colPrDealer}"/>
                </Column>
                <Column>
                    <Text text="{i18n>colViewDate}"/>
                </Column>
                <Column>
                    <Text text="{i18n>colEarnedPoint}"/>
                </Column>
                <!-- <Column>
                    <Text text="{i18n>colStatus}"/>
                </Column> -->
            </columns>

            <items>
                <ColumnListItem>
                    <cells>
                        <Text text="{PainterDetails/Name}"/>
                        <Text text="{PainterDetails/MembershipCard}"/>
                        <Text text="{PainterDetails/Mobile}"/>
                        <Text text="{PainterDetails/ZoneId}"/>
                        <Text text="{PainterDetails/DivisionId}"/>
                        <Text text="{PainterDetails/DepotId}"/>
                        <Text text="{PainterDetails/PrimaryDealerDetails/DealerName}"/>
                        <Text text="{path:'CreatedAt',type:'sap.ui.model.type.Date',formatOptions:{pattern:'dd/MM/YYYY'}}"/>
                        <Text text="{EarnedPoints}"/>
                        <!-- <ObjectStatus text="{path:'Status',formatter:'.fmtStatus'}" state="{= ${Status} === 'ENROLLED' ? 'Success' : 'None' }"/> -->
                    </cells>
                </ColumnListItem>
            </items>
        </Table>

    </VBox>
</core:FragmentDefinition>
